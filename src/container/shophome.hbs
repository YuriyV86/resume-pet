<div>
  <main>
    {{! navigation  }}
    <section>
      <div class='card'>
        <div class='nav'>
          {{#with navigation/links}}
            {{#each this}}
              <div class='nav-item mx-2'>
                {{>nav-item href=href text=text }}
              </div>
            {{/each}}

            <div class='nav-item mx-2'>
              {{>nav-item href='#' text='Disabled' state='disabled'}}
            </div>
          {{/with}}
        </div>
      </div>
    </section>

    {{! newsBlock }}
    <section>
      {{#with newsBlock}}
        <p class='h1 text-center'>{{title}}</p>

        <div class='row'>
          {{#each cards}}
            <div class='col-3 g-2'>

              <div class='card text-bg-dark h-100'>
                <div class='card-header m-1 p-1'>
                  <h6>{{this.title}}
                    {{#if isNew}}
                      <span
                        class='badge rounded-pill card text-bg-success m-1 p-1'
                      >NEW</span>
                    {{/if}}

                    {{#if isTop}}
                      <span
                        class='badge rounded-pill card text-bg-danger m-1 p-1'
                      >TOP</span>
                    {{/if}}
                  </h6>
                </div>

                <div class='card-body p-1 pb-4'>
                  {{this.description}}
                </div>
              </div>

            </div>
          {{/each}}
        </div>
      {{/with}}
    </section>
    <hr class='my-4' />

    {{! goodsBlock }}
    <section>
      {{#with goodsBlock}}
      <div class="row">
        
          <div class='nav nav-tabs'>
          {{#each tabs}}
            <div class='nav-item'>
              <a
                class='nav-link 
                  {{#if isActive}}active{{/if}} 
                  {{#if isDisabled}}disabled{{/if}}' 
                href='#'
              >{{text}}</a>
            </div>
          {{/each}}

          </div>

        {{#with cards}}
          {{#each this}}
            <div class="col-4 gx-2 gy-5">
            <div class="card h-100">
              <div class="card-body p-0">
                <img class="card-img-top" src="{{image}}" alt="{{image}}">

                <div class='card-title ps-2  pt-2'>
                  <h6>{{title}}
                    {{#if isHot}}
                      <span
                        class='badge rounded-pill card text-bg-warning p-1'
                      >HOT</span>
                    {{/if}}

                    {{#if isNew}}
                      <span
                        class='badge rounded-pill card text-bg-success p-1'
                      >NEW</span>
                    {{/if}}
                  </h6>
                </div>

                {{!-- <h6 class='card-title'>{{title}}</h6> --}}
                <p class=" p-1 pb-4">{{description}} </p>
              </div>
            </div>
            </div>
          {{/each}}
        {{/with}}
        
      </div>
      {{/with}}
    </section>
    <hr class='mt-3 mb-3' />

    {{!-- subscribe --}}
    <section>      
      {{#with subscribe}}
        <div class="alert text-bg-dark p-2">
          <p class='h2'>{{header}}</p>
          <p>{{description}}</p>
          <div class="col-3">
            {{#each buttons}}
              <a href="{{link}}" ><div class="btn btn-primary m-2">{{text}}</div></a> 
            {{/each}}
          </div>
        </div>
      {{/with}}
    </section>
    <hr  class='mt-4 mb-3' />

    {{!-- service --}}
    <section>      
      {{#with service}}
        <div class="alert text-bg-success text-center p-2">
          <p class='h2'>{{title}}</p>
          <p>{{description}}</p>
          {{#each buttons}}
            <a href="{{link}}" ><div class="btn btn-primary my-2">{{text}}</div></a> 
          {{/each}}
        </div>
      {{/with}}
    </section>
    <hr  class='mt-4 mb-1' />

    {{!-- footer --}}
    <section>
      <div class='nav'>
        {{#each footer}}
          <div class='col-2'>
            <div class='nav-item p-2'>
              {{>nav-item href=this.[0].link text=this.[0].text }}
              {{>nav-item href=this.[1].link text=this.[1].text }}
              {{>nav-item href=this.[2].link text=this.[2].text }}
              {{>nav-item href=this.[3].link text=this.[3].text state='disabled'}}
            </div>
          </div>
        {{/each}}
      </div>
    </section>

  </main>
</div>
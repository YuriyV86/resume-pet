<div class="ps-0">
  <div class='nav'>
    {{#each footer}}
      <div class='col-2'>

        <div class='nav-item py-2'>
          {{#each this}}

            {{#if @last}}
              {{> nav-item href=link text=text state='disabled'}}
            {{else}}
              {{>nav-item href=link text=text }}
            {{/if}}

          {{/each}}
        </div>

      </div>
    {{/each}}
  </div>
</div>